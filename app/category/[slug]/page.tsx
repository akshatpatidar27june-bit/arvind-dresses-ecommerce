'use client';
import {useEffect,useMemo,useState} from 'react';
import {useParams} from 'next/navigation';
import {supabase} from '../../../lib/supabase';

type Product={id:string;name:string;price:number;compare_at_price:number|null;images:string[]|null;stock:number;is_active:boolean};
type Category={id:string;name:string;description:string|null;image_url:string|null;is_active:boolean};
export default function CategoryPage(){const params=useParams<{slug:string}>();const slug=String(params.slug||'');const [category,setCategory]=useState<Category|null>(null),[products,setProducts]=useState<Product[]>([]),[loading,setLoading]=useState(true),[sort,setSort]=useState('newest'),[maxPrice,setMaxPrice]=useState('');useEffect(()=>{(async()=>{setLoading(true);const {data:cat}=await supabase.from('categories').select('id,name,description,image_url,is_active').eq('slug',slug).eq('is_active',true).maybeSingle();if(!cat){setLoading(false);return}setCategory(cat as Category);const {data}=await supabase.from('products').select('id,name,price,compare_at_price,images,stock,is_active').eq('category_id',cat.id).eq('is_active',true).order('created_at',{ascending:false});setProducts((data??[]) as Product[]);setLoading(false)})()},[slug]);const visible=useMemo(()=>{let p=products.filter(x=>!maxPrice||Number(x.price)<=Number(maxPrice));if(sort==='low')p.sort((a,b)=>Number(a.price)-Number(b.price));if(sort==='high')p.sort((a,b)=>Number(b.price)-Number(a.price));return p},[products,sort,maxPrice]);return <main className="category-page"><header className="cat-header"><a href="/">← Arvind Dresses</a>{category&&<><p>COLLECTION</p><h1>{category.name}</h1><span>{category.description||'Explore the latest styles in this collection.'}</span></>}</header>{category&&<div className="cat-controls"><span>{visible.length} products</span><div><label>Price <select value={maxPrice} onChange={e=>setMaxPrice(e.target.value)}><option value="">Any</option><option value="1000">Under ₹1,000</option><option value="2500">Under ₹2,500</option><option value="5000">Under ₹5,000</option><option value="10000">Under ₹10,000</option></select></label><label>Sort <select value={sort} onChange={e=>setSort(e.target.value)}><option value="newest">Newest</option><option value="low">Price: Low to high</option><option value="high">Price: High to low</option></select></label></div></div>}{loading?<p className="empty">Loading collection…</p>:!category?<div className="empty"><h2>Collection not found</h2><a href="/">Back to store</a></div>:visible.length===0?<div className="empty"><h2>No products in this collection yet.</h2><p>Check back soon for new arrivals.</p></div>:<div className="grid">{visible.map(p=><article key={p.id}><a href={`/product/${p.id}`} className="pic">{p.images?.[0]?<img src={p.images[0]} alt={p.name}/>:<span>ARVIND</span>}<b>{p.stock>0?'AVAILABLE':'SOLD OUT'}</b></a><a href={`/product/${p.id}`}><h2>{p.name}</h2></a><div><strong>₹{Number(p.price).toLocaleString('en-IN')}</strong>{p.compare_at_price&&<del>₹{Number(p.compare_at_price).toLocaleString('en-IN')}</del>}</div></article>)}</div>}<style jsx>{`*{box-sizing:border-box}.category-page{min-height:100vh;background:#fbfaf8;color:#171614;padding:30px 5vw 90px;font-family:Arial,Helvetica,sans-serif}.cat-header{max-width:1100px;margin:auto;padding:20px 0 38px;border-bottom:1px solid #ddd6cf}.cat-header>a{color:#77706a;text-decoration:none;font-size:11px}.cat-header p{margin:55px 0 8px;font-size:9px;letter-spacing:2px;color:#9a9087}.cat-header h1{font-size:clamp(42px,7vw,82px);letter-spacing:-4px;margin:0 0 14px}.cat-header span{font-size:13px;color:#817a73}.cat-controls{max-width:1100px;margin:22px auto;display:flex;justify-content:space-between;align-items:center;font-size:11px;color:#817a73}.cat-controls>div{display:flex;gap:14px}.cat-controls label{display:flex;align-items:center;gap:6px}.cat-controls select{border:1px solid #d8d0c8;background:white;padding:9px 28px 9px 10px;font-size:10px}.grid{max-width:1100px;margin:25px auto;display:grid;grid-template-columns:repeat(4,1fr);gap:26px 18px}.pic{display:block;aspect-ratio:3/4;background:#eee7df;position:relative;overflow:hidden}.pic img{width:100%;height:100%;object-fit:cover}.pic span{display:grid;place-items:center;height:100%;font-size:12px;letter-spacing:2px;color:#9c7251}.pic b{position:absolute;left:10px;top:10px;background:#171614;color:#fff;padding:6px 8px;font-size:7px;letter-spacing:1px}.grid article>a:not(.pic){text-decoration:none;color:#171614}.grid h2{font-size:13px;margin:12px 0 8px;font-weight:600}.grid article>div{font-size:12px}.grid strong{font-size:14px}.grid del{color:#999;margin-left:8px}.empty{text-align:center;padding:80px 20px;color:#817a73}.empty h2{color:#171614}@media(max-width:800px){.grid{grid-template-columns:repeat(2,1fr)}.cat-controls{align-items:flex-start;gap:15px;flex-direction:column}.cat-controls>div{flex-wrap:wrap}}`}</style></main>}
