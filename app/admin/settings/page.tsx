'use client';
import {useEffect,useState} from 'react';
import {supabase} from '../../../lib/supabase';
type Settings={id:number;store_name:string;phone:string|null;whatsapp:string|null;email:string|null;address:string|null;currency:string;free_delivery_threshold:number|null;delivery_charge:number|null};type Staff={id:string;email:string;full_name:string;role:string;is_active:boolean};
export default function Settings(){const [s,setS]=useState<Settings>({id:1,store_name:'Arvind Dresses',phone:'',whatsapp:'',email:'',address:'',currency:'INR',free_delivery_threshold:null,delivery_charge:null}),[staff,setStaff]=useState<Staff[]>([]),[name,setName]=useState(''),[email,setEmail]=useState(''),[role,setRole]=useState('cashier'),[message,setMessage]=useState(''),[saving,setSaving]=useState(false);async function load(){const [{data:a,error:e},{data:b,error:e2}]=await Promise.all([supabase.from('store_settings').select('*').eq('id',1).maybeSingle(),supabase.from('staff_profiles').select('*').order('created_at',{ascending:false})]);if(e||e2)setMessage((e||e2)?.message||'');if(a)setS(a as Settings);setStaff((b??[]) as Staff[])}useEffect(()=>{load()},[]);async function save(){setSaving(true);const {error}=await supabase.from('store_settings').upsert({...s,id:1});setMessage(error?.message||'Settings saved.');setSaving(false)}async function addStaff(e:React.FormEvent){e.preventDefault();setSaving(true);const {error}=await supabase.from('staff_profiles').insert({full_name:name.trim(),email:email.trim().toLowerCase(),role,is_active:true});if(error)setMessage(error.message);else{setName('');setEmail('');setMessage('Staff profile added. Create/invite the matching Auth user before they can sign in.');await load()}setSaving(false)}async function toggle(x:Staff){const {error}=await supabase.from('staff_profiles').update({is_active:!x.is_active}).eq('id',x.id);if(error)setMessage(error.message);else load()}return <main className="admin-page"><div className="admin-page-head"><p className="eyebrow">ARVIND DRESSES / CONTROL</p><h1>Settings</h1><p>Store configuration and staff access.</p></div>{message&&<div className="admin-message">{message}</div>}<section className="panel"><div className="panel-head"><div><h2>Store settings</h2><p>Business details used across the store.</p></div><button className="primary-btn" onClick={save} disabled={saving}>{saving?'Saving…':'Save settings'}</button></div><div className="settings-grid"><label>Store name<input value={s.store_name} onChange={e=>setS({...s,store_name:e.target.value})}/></label><label>Phone<input value={s.phone||''} onChange={e=>setS({...s,phone:e.target.value})}/></label><label>WhatsApp<input value={s.whatsapp||''} onChange={e=>setS({...s,whatsapp:e.target.value})}/></label><label>Email<input type="email" value={s.email||''} onChange={e=>setS({...s,email:e.target.value})}/></label><label className="full">Address<textarea value={s.address||''} onChange={e=>setS({...s,address:e.target.value})}/></label><label>Currency<input value={s.currency} onChange={e=>setS({...s,currency:e.target.value})}/></label><label>Free delivery above ₹<input type="number" value={s.free_delivery_threshold??''} onChange={e=>setS({...s,free_delivery_threshold:e.target.value===''?null:Number(e.target.value)})}/></label><label>Delivery charge ₹<input type="number" value={s.delivery_charge??''} onChange={e=>setS({...s,delivery_charge:e.target.value===''?null:Number(e.target.value)})}/></label></div></section><section className="panel"><div className="panel-head"><div><h2>Staff & access</h2><p>Set up profiles for owner, manager, cashier and order packer.</p></div></div><form className="staff-form" onSubmit={addStaff}><input placeholder="Full name" value={name} onChange={e=>setName(e.target.value)} required/><input type="email" placeholder="Staff email" value={email} onChange={e=>setEmail(e.target.value)} required/><select value={role} onChange={e=>setRole(e.target.value)}><option value="owner">Owner</option><option value="manager">Manager</option><option value="cashier">Cashier</option><option value="order_packer">Order packer</option></select><button className="primary-btn" disabled={saving}>Add staff</button></form><div className="admin-list">{staff.map(x=><div className="admin-list-row" key={x.id}><div><strong>{x.full_name}</strong><small>{x.email} · {x.role.replace('_',' ')} · {x.is_active?'Active':'Disabled'}</small></div><button className="text-btn" onClick={()=>toggle(x)}>{x.is_active?'Disable':'Enable'}</button></div>)}</div></section><style jsx>{`.admin-page{padding:42px;max-width:1200px}.admin-page-head{margin-bottom:25px}.admin-page h1{font-size:42px;margin:5px 0}.admin-page-head>p:not(.eyebrow){color:#817a73;font-size:12px}.settings-grid{display:grid;grid-template-columns:1fr 1fr;gap:15px;padding:20px}.settings-grid label{font-size:10px;text-transform:uppercase;letter-spacing:.7px;color:#716a63;font-weight:700}.settings-grid input,.settings-grid textarea,.staff-form input,.staff-form select{display:block;width:100%;margin-top:7px;border:1px solid #ddd5cd;padding:12px;font:12px Arial;background:#fff}.settings-grid textarea{min-height:75px;resize:vertical}.full{grid-column:1/-1}.staff-form{display:grid;grid-template-columns:1fr 1fr 180px auto;gap:10px;padding:20px}.admin-list{padding:0 20px}.admin-list-row{display:flex;justify-content:space-between;align-items:center;padding:16px 0;border-bottom:1px solid #eee8e2}.admin-list-row strong{display:block}.admin-list-row small{display:block;color:#8b837b;margin-top:4px;text-transform:capitalize}.text-btn{background:none;border:0;cursor:pointer;padding:8px}@media(max-width:800px){.admin-page{padding:25px}.settings-grid,.staff-form{grid-template-columns:1fr}.full{grid-column:auto}.admin-page h1{font-size:34px}}`}</style></main>}
